#the outline for the model used for the database

class Model():
    def single_select(self, title=None, author=None):
        """
        INPUT: title(str|None), author(str|None)
        Search the datastore and return matching books.
        """
        pass

    def insert(self, title, author_first, author_last, series, genre, version, publish_date, publisher, entry_date):

        pass


